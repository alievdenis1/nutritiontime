// TODO: тут будет лежать функционал, который будет отвечать за фильтрацию рецептов
// Но нужно ещё подумать

// TODO: а может и не нужно отделять recipes и filter, возможно в одном сторе сделать

import { defineStore } from 'pinia'

export const useSearchFilters = defineStore('search-filters', () => {
//   const route = useRoute()
  // this assumes `app.provide('appProvided', 'value')` was called
//   const appProvided = inject('appProvided')

  // ...

  return {
    // ...
  }
})